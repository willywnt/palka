// Test stub for the `server-only` package, which intentionally throws when imported
// outside a React Server Component bundle. Aliased in vitest.config.ts so that
// server-only services can be unit-tested in a plain Node environment.
export {};
